IMPORTANT — unrelated to this footer fix:

src/App.tsx in the uploaded repo currently imports Home Plate's pages/routes
(WeeklyPlannerPage, EditMealPage, ShoppingListPage, /library/:mealId/cook, etc.)
instead of Media Journal's real pages (TimelinePage, DashboardPage, AddEntryPage,
EditEntryPage, StatisticsPage, InProgressPage, LibraryPage, TraktCallbackPage,
MalCallbackPage).

This was true in two separate zip uploads, so it looks like it's actually
checked into the repo, not a one-off bad export. Worth checking:
  1. Is this committed and pushed, or just a local/uncommitted change?
  2. If pushed, is your live deployed site actually broken right now, or is
     the live dist/ build from an older, correct commit?
  3. `git log -- src/App.tsx` should show when/how this happened.

I didn't touch App.tsx — didn't want to guess at MJ's real route paths/params
without you confirming. The AppLayout.tsx footer fix in this zip was verified
against a correct stand-in App.tsx, then that stand-in was discarded — only
AppLayout.tsx is included here.
