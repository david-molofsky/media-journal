import { PagePlaceholder } from '@/components/common/PagePlaceholder';

/**
 * Settings — import/export, media type management and about info
 * (PRD section 5, UI & UX Specification section 9).
 *
 * Implemented in Milestone 7.
 */
export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Import, export and manage your Media Journal preferences here."
    />
  );
}
