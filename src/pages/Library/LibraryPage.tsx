import { PagePlaceholder } from '@/components/common/PagePlaceholder';

/**
 * Library — the permanent, searchable archive of every entry.
 *
 * Will support instant search, filtering by year/month/media type and
 * sorting (PRD section 5, UI & UX Specification section 5). Implemented
 * in Milestone 4.
 */
export default function LibraryPage() {
  return (
    <PagePlaceholder
      title="Library"
      description="Search, filter and browse your complete media archive here."
    />
  );
}
