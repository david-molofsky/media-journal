import { PagePlaceholder } from '@/components/common/PagePlaceholder';

/**
 * Edit Entry — same form as Add Entry, pre-populated, with delete and
 * duplicate actions (UI & UX Specification section 7).
 *
 * Implemented in Milestone 3.
 */
export default function EditEntryPage() {
  return (
    <PagePlaceholder
      title="Edit Entry"
      description="Update, duplicate or delete an existing entry here."
    />
  );
}
