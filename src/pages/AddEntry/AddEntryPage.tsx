import { PagePlaceholder } from '@/components/common/PagePlaceholder';

/**
 * Add Entry — media type selection followed by the appropriate form.
 *
 * Implemented in Milestone 3.
 */
export default function AddEntryPage() {
  return (
    <PagePlaceholder
      title="Add Entry"
      description="Choose a media type and record what you've finished."
    />
  );
}
